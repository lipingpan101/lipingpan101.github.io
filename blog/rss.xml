<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:content="http://purl.org/rss/1.0/modules/content/">
    <channel>
        <title>Liping's Site Blog</title>
        <link>https://lipingpan101.github.io/blog</link>
        <description>Liping's Site Blog</description>
        <lastBuildDate>Sat, 25 Feb 2023 00:00:00 GMT</lastBuildDate>
        <docs>https://validator.w3.org/feed/docs/rss2.html</docs>
        <generator>https://github.com/jpmonette/feed</generator>
        <language>en</language>
        <item>
            <title><![CDATA[Git Basics]]></title>
            <link>https://lipingpan101.github.io/blog/Git Basics</link>
            <guid>https://lipingpan101.github.io/blog/Git Basics</guid>
            <pubDate>Sat, 25 Feb 2023 00:00:00 GMT</pubDate>
            <description><![CDATA[Here are some commonly used Git commands:]]></description>
            <content:encoded><![CDATA[<p><strong>Here are some commonly used Git commands:</strong></p><p><strong>Introduction Sequence</strong></p><ol><li><code>git commit</code>: records changes to the repository</li><li><code>git commit --amend</code>: modifies the last commit</li><li><code>git branch newImage</code>: creates a new branch named newImage</li><li><code>git checkout &lt;name&gt;</code>: switches to an existing branch</li><li><code>git checkout -b [yourbranchname]</code>: creates a new branch and switches to it</li><li><code>git merge bugFix</code>: merges the bugFix branch into the current branch</li><li><code>git rebase main</code>: applies the changes from the current branch on top of the main branch</li></ol><p><strong>Ramping Up</strong></p><ol start="8"><li><code>HEAD</code>: the symbolic name for the currently checked out commit</li><li>Detaching <code>HEAD</code>: attaching it to a commit instead of a branch</li><li>Moving upwards one commit at a time with <code>^</code>. Moving upwards a number of times with <code>~&lt;num&gt;</code>, e.g., <code>git checkout HEAD~4</code>.</li><li>Reassigning a branch to a commit with the <code>-f</code> option, e.g., <code>git branch -f main HEAD~3</code>, moves the main branch to three parents behind <code>HEAD</code>.</li><li><code>git reset</code>: moves a branch backwards as if the commit had never been made. Use git revert instead to reverse changes and share those reversed changes with others.</li></ol><p><strong>Moving Work Around</strong></p><ol start="13"><li><code>git cherry-pick &lt;commit1&gt; &lt;commit2&gt; &lt;...&gt;</code>: copies a series of commits below your current location (HEAD)</li><li><code>git rebase -i</code>: opens up a UI to show you which commits are about to be copied below the target of the rebase, e.g., <code>git rebase -i HEAD~4</code></li></ol><p><strong>A Mixed Bag</strong></p><ol start="15"><li><code>git tag v1 C1</code>: names the tag v1 and references the commit C1 explicitly</li><li><code>git describe &lt;ref&gt;</code>: describes the commit referenced by <code>&lt;ref&gt;</code> in the form of <code>&lt;tag&gt;_&lt;numCommits&gt;_g&lt;hash&gt;</code></li></ol><p><strong>Advanced Topics</strong></p><ol start="17"><li>The ^ modifier also accepts an optional number after it, which specifies which parent reference to follow from a merge commit.</li></ol><p><strong>Push &amp; Pull -- Git Remotes</strong></p><ol start="18"><li><code>git clone</code>: creates a copy of a remote repository on your local machine.</li><li>Remote branches are on your local repository, not on the remote repository.</li><li>Remote branches have a naming convention in the format of <code>&lt;remote name&gt;/&lt;branch name&gt;</code>, e.g., origin/main.</li><li><code>git fetch</code>: downloads changes from the remote repository but does not integrate them into your local repository.</li><li><code>git pull</code>: downloads changes from the remote repository and integrates them into your local repository.</li><li><code>git fakeTeamwork &lt;branch&gt; &lt;numCommits&gt;</code>: simulates a teammate pushing commits to the <code>&lt;branch&gt;</code> branch on the remote.</li><li><code>git push</code>: uploads changes from your local repository to the remote repository.</li></ol><p><strong>To Origin And Beyond -- Advanced git Remotes</strong></p><ol start="25"><li><p><code>git checkout -b branchname origin/branchname</code>: creates a new local branch from a remote branch.</p></li><li><p><code>git branch -u origin/main foo</code>: sets the foo branch to track origin/main.</p></li><li><p><code>git push &lt;remote&gt; &lt;place&gt;</code>: pushes the changes in <code>&lt;place&gt;</code> to the specified <code>&lt;remote&gt;</code>.</p></li><li><p><code>git push origin &lt;source&gt;:&lt;destination&gt;</code>: pushes changes from the <code>&lt;source&gt;</code> branch to the <code>&lt;destination&gt;</code> branch on the origin remote.</p></li><li><p><code>git fetch origin &lt;source&gt;:&lt;destination&gt;</code>: fetches changes from the <code>&lt;source&gt;</code> branch on the `</p></li></ol>]]></content:encoded>
            <category>Command Line</category>
            <category>Git</category>
        </item>
    </channel>
</rss>